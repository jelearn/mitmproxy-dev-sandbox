# Editors
*~
*.swp
*.swo
.vscode

# Link to the sandbox workspace volume
sandbox
# Local files to load into the sandbox workspace volume
workspace

# local settings
.env
mitmproxy-sandbox-ca.pem

# Claude Code local settings (machine-specific, not for version control)
.claude
